import React from "react";
import ReactDOM from "react-dom/client";
import TerritoryPrototype from "./TerritoryPrototype.jsx";
import "./index.css";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <TerritoryPrototype />
  </React.StrictMode>
);
